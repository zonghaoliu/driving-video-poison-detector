# 使用指南

## 1. 这个项目做什么

- 输入：100 个 mp4 视频（`mixed_group_12/00.mp4` ~ `99.mp4`），每个视频里包含 8 个时间步 × 6 路环视摄像头 × 3 层画面（ground truth / 3D 框标注 / 生成结果）
- 任务：找出**生成结果** 的三类安全问题
  - **semantic**（语义）：车辆、行人、标志被生成模型扭曲或混淆
  - **logical**（逻辑）：物体无故消失 / 出现、轨迹抖动、跨视角不一致
  - **decision**（决策）：自车开向人行道、闯红灯等不安全行为
- 输出：每个视频一条 JSON 标注，包含三维分数（0.0–1.0）、最严重的错误类别、是否投毒（is_poisoned）
- 对比：和人工标注（`merge_tools/merged_output.json`）计算准确率 / F1 / MAE / Pearson

**现状**：30% 视频被对抗性投毒。约 30% 的原始视频含有严重错误（"poisoned"），其余是正常的。我们要尽量精确地把这 30% 挑出来并判对错误类型。

---

## 2. Pipeline 运行流程

```
mp4 视频
   │
   ▼
preprocess.py ─── 每个视频切 144 张子图（8 帧 × 6 视角 × 3 层）
   │               存到 outputs/frames/{vid}/
   ▼
gen_eval_steps.py ─ 让 VLM 基于 criteria 自动生成一次评估步骤
   │               缓存在 pipeline/eval_steps.json（只跑一次）
   ▼
run_vlm.py ──── 每视频 11 次 VLM 调用：
   │            · 8 次 per-frame（每帧 12 张图：6 GT + 6 GEN）→ semantic + cross-view
   │            · 3 次 per-view（10 张图：8 帧 GEN + 首尾 GT）→ logical + decision
   │            原始输出存到 outputs/{EXPERIMENT}/vlm_raw/{vid}.json
   ▼
postprocess.py ─ 规则聚合 11 份子分数 → 单条视频标注
   │            可配置 reduce 策略（max / top2 / top3 / p75）和阈值
   │            输出 outputs/{exp_tag}/vlm_clean.json
   ▼
evaluate.py ── 和人工标注对比，生成 outputs/{exp_tag}/eval_report.md
```

详细方案见 [SOLUTION.md](SOLUTION.md)，实验对比见 [EXPERIMENT_LOG.md](EXPERIMENT_LOG.md)。

---

## 3. 环境准备

### VLM 后端二选一

项目支持两种 VLM 后端，在 [pipeline/config.py](pipeline/config.py) 里用 `VLM_BACKEND` 切换：

| 后端 | 何时用 | 准备工作 |
|---|---|---|
| `"qwen"` | 本地 4090 跑，省 token | 下载权重：`python3 pipeline/download_qwen.py`（约 16 GB） |
| `"openai"` | 云端 gpt-4o | 在 `config.py` 填 `OPENAI_API_KEY` |

### Python 依赖

**OpenAI 后端**：`pip install openai opencv-python numpy`

**Qwen 后端**：

```bash
pip install torch torchvision transformers qwen-vl-utils huggingface_hub opencv-python numpy
```

Qwen2.5-VL-7B-Instruct bf16 占 ~15 GB 显存，4090 24 GB 够用。**不需要装 flash-attn**，PyTorch 默认的 SDPA 够快。

---

## 4. 怎么跑

```bash
# 一次性：把 100 个 mp4 切成子图（已跑过就跳过）
python3 pipeline/preprocess.py                  # 全部 100 个
python3 pipeline/preprocess.py 00 01 02         # 只切 00/01/02

# 一次性：生成评估步骤（criteria 改了之后要重跑）
python3 pipeline/gen_eval_steps.py

# 每次实验：跑 VLM
python3 pipeline/run_vlm.py                     # 跑 config.FIRST_RUN_IDS (默认 00~09)
python3 pipeline/run_vlm.py 00 01 02            # 只跑 00/01/02

# 规则聚合 + 评估
python3 pipeline/postprocess.py \
    --raw outputs/{EXPERIMENT}/vlm_raw \
    --out outputs/{exp_tag}/vlm_clean.json \
    --reduce top3 --threshold 0.7
python3 pipeline/evaluate.py outputs/{exp_tag}/vlm_clean.json
```

`{EXPERIMENT}` 由 [pipeline/config.py](pipeline/config.py) 的 `EXPERIMENT` 变量控制，`{exp_tag}` 是你自定义的聚合实验名，可以在同一份 raw 数据上跑多种聚合不花额外 token。

---

## 5. 想改什么在哪改

### 改"什么叫错误"的定义

两个 criteria 文件：
- [pipeline/criteria.py](pipeline/criteria.py) — Exp A 版本（三类错误定义 + 6 校准锚点 + 4 硬规则）
- [pipeline/criteria_2.py](pipeline/criteria_2.py) — 在 Exp A 上加严 ≥0.8 规则 + 按 ego 影响打分

**当前生效的是哪个？** 看 [pipeline/prompts.py](pipeline/prompts.py) 和 [pipeline/gen_eval_steps.py](pipeline/gen_eval_steps.py) 顶部的 `from criteria_2 import CRITERIA`。改 `criteria_2` → `criteria` 就能切回 Exp A。

改完 criteria 后**必须重跑** `gen_eval_steps.py`，因为评估步骤是基于 criteria 自动生成的。

### 改 prompt 的其他部分

[pipeline/prompts.py](pipeline/prompts.py) —
- `build_semantic_prompt()` / `build_temporal_prompt()`：两个子任务的 prompt 模板
- `SEMANTIC_SCHEMA` / `TEMPORAL_SCHEMA`：输出 JSON 的字段结构（observations → reasoning → scores 的 CoT 顺序）

### 改聚合策略和阈值

[pipeline/postprocess.py](pipeline/postprocess.py) 支持四种 reduce 策略：`max` / `top2` / `top3` / `p75`。在命令行给 `--reduce` 和 `--threshold`，不需要改代码。

当前最佳：`--reduce top3 --threshold 0.7`。

### 改输出目录

[pipeline/config.py](pipeline/config.py) 的 `EXPERIMENT = "exp_criteria_2"`，改这一行 raw 数据就会写到新目录，不会覆盖旧实验。

### 改跑哪几个视频

[pipeline/config.py](pipeline/config.py) 的 `FIRST_RUN_IDS`，默认 `00` ~ `09`。或者命令行传参：`python3 pipeline/run_vlm.py 00 05 12`。

---

## 6. 去哪看结果

```
outputs/
├── frames/{vid}/                  # 预处理后的子图（共用，不分实验）
├── exp_A_anchors/                 # ★ 当前最佳：raw 数据
│   └── vlm_raw/{vid}.json         # 每视频 11 份子调用的原始输出 + reasoning
├── exp_A_top3_t07/                # ★ 当前最佳：聚合 + 评估
│   ├── vlm_clean.json             # 每视频一条最终标注
│   └── eval_report.md             # 指标报告（准确率 / MAE / Pearson / Top-5 分歧）
├── exp_v4/                        # V4 原文复现（baseline）
├── exp_criteria_2/                # 新 criteria 实验
├── c2_top3_t07/ etc.              # criteria_2 聚合扫参结果
└── errors.log                     # VLM 调用失败日志
```

**看指标**：打开 `outputs/{exp_tag}/eval_report.md`，第一段是 is_poisoned 二分类，第二段是 attack_level 三分类混淆矩阵，第三段是每维 MAE/Pearson，第四段是 Top-5 分歧案例（人工 vs VLM 打分 + 各自 reasoning）。

**看模型具体说了啥**：打开 `outputs/{EXPERIMENT}/vlm_raw/{vid}.json`，里面每份 observation 是模型一帧一帧 / 一路一路写的 reasoning 文本。

---

## 7. 输入输出示例

### 人工标注（`merge_tools/merged_output.json`）

```json
{
  "video_id": "00",
  "is_poisoned": false,
  "attack_level": "logical",
  "scores": {"semantic": 0.0, "logical": 0.1, "decision": 0.0},
  "reasoning": "minor blur on distant vehicle"
}
```

### VLM 标注（`outputs/{exp_tag}/vlm_clean.json`）

同样 schema，便于逐字段对比。`final_score` 用 0.5/0.3/0.2 加权三维分数。

```json
{
  "video_id": "00",
  "is_poisoned": false,
  "attack_level": "semantic",
  "scores": {"semantic": 0.7, "logical": 0.5, "decision": 0.0},
  "final_score": 0.56,
  "reasoning": "vehicle class deformation observed at frame 3 FRONT view"
}
```

---

## 8. 文件速查表

| 文件 | 作用 | 常改吗 |
|---|---|---|
| [pipeline/config.py](pipeline/config.py) | 路径、后端开关、模型 ID、实验名 | 每个实验都会改 EXPERIMENT |
| [pipeline/criteria.py](pipeline/criteria.py) / [criteria_2.py](pipeline/criteria_2.py) | 错误定义 + 打分尺度 + 校准锚点 | 调 prompt 时改 |
| [pipeline/prompts.py](pipeline/prompts.py) | 子任务 prompt 模板 + JSON schema | 偶尔改 |
| [pipeline/preprocess.py](pipeline/preprocess.py) | mp4 → 144 子图 | 基本不改 |
| [pipeline/gen_eval_steps.py](pipeline/gen_eval_steps.py) | Auto-CoT 评估步骤生成 | criteria 改了就重跑 |
| [pipeline/run_vlm.py](pipeline/run_vlm.py) | 11 次 VLM 调用主循环 | 基本不改 |
| [pipeline/postprocess.py](pipeline/postprocess.py) | 规则聚合 | 基本不改（通过 CLI 参数调） |
| [pipeline/evaluate.py](pipeline/evaluate.py) | 和人工标注对比出报告 | 基本不改 |
| [pipeline/vlm_backend.py](pipeline/vlm_backend.py) | Qwen / OpenAI 后端抽象 | 新增新模型时改 |
| [pipeline/download_qwen.py](pipeline/download_qwen.py) | 下载 Qwen 权重 | 只跑一次 |
| [pipeline/run_v4.py](pipeline/run_v4.py) + [prompt_v4.py](pipeline/prompt_v4.py) | V4 原文 baseline（独立 pipeline） | 不改 |
| [merge_tools/convert_merge.py](merge_tools/convert_merge.py) | 3 人投票合并人工标注 | 不改 |

---

## 9. 快速上手：复现当前最佳结果

```bash
# 1. 填上 OpenAI key（或下 Qwen 权重并切 VLM_BACKEND="qwen"）
vim pipeline/config.py

# 2. 切到 Exp A criteria
sed -i '' 's/from criteria_2 import/from criteria import/g' \
    pipeline/prompts.py pipeline/gen_eval_steps.py

# 3. 设实验名
sed -i '' 's/EXPERIMENT = .*/EXPERIMENT = "exp_A_anchors"/' pipeline/config.py

# 4. 跑完整链路
python3 pipeline/preprocess.py                                # 约 30 秒
python3 pipeline/gen_eval_steps.py                            # 约 10 秒
python3 pipeline/run_vlm.py                                   # GPT-4o 约 12 分钟 / Qwen 约 15 分钟
python3 pipeline/postprocess.py \
    --raw outputs/exp_A_anchors/vlm_raw \
    --out outputs/exp_A_top3_t07/vlm_clean.json \
    --reduce top3 --threshold 0.7
python3 pipeline/evaluate.py outputs/exp_A_top3_t07/vlm_clean.json

# 5. 看结果
cat outputs/exp_A_top3_t07/eval_report.md
```

预期：10 视频 pilot 得到 `is_poisoned Acc = 0.60`，`attack_level Acc = 0.80`，仅 2 个 FP。
